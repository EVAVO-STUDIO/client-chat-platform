# Defectra Expo SDK 52 frozen dependency graph

This directory is a public, immutable recovery snapshot for the private `EVAVO-STUDIO/defectra` workspace.

The lock was resolved with Node 22.16.0 and pnpm 10.20.0 from checksum-pinned Defectra root, API, workspace and npm configuration manifests. Generation performed a second `pnpm install --frozen-lockfile --ignore-scripts` before publishing.

`receipt.json` records the source manifest blob SHAs, lock SHA-256, byte length and the governed direct dependency specifiers. The snapshot contains package metadata only and no credentials, source files or runtime configuration.

This artifact is recovery/build input, not release evidence.
